# pubspec.yaml
name: echo_see_companion
description: EchoSee Smart Glasses Companion App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  provider: ^6.0.5
  supabase_flutter: ^2.5.6
  google_sign_in: ^6.2.1
  # Animations
  lottie: ^2.7.0
  animated_text_kit: ^4.2.2
  
  # UI Components
  flutter_svg: ^2.0.9
  flutter_animate: ^4.1.2
  shimmer: ^3.0.0
  
  # Storage
  sqflite: ^2.3.0
  shared_preferences: ^2.2.2
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.15.0
  
  # Utilities
  intl: ^0.18.1
  uuid: ^4.4.0
  path_provider: ^2.1.0
  url_launcher: ^6.2.0
  
  # Audio/Speech
  speech_to_text: ^7.3.0
  audio_session: ^0.1.11
  just_audio: ^0.9.35
  
  # Charts & Graphs
  fl_chart: ^0.66.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  mocktail: ^1.0.4

flutter:
  uses-material-design: true
